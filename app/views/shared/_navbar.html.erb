<nav class="navbar navbar-expand-lg navbar-dark navbar-bg sticky-top navbar-lactorun">
    <div class = "container">
        
        <!-- Logo e Título-->
        <a class="navbar-brand font-comfortaa" href=<%= root_path %>>
            <img src="/assets/logos/logo.png" width="30" height="30" class="d-inline-block align-top" alt="">
            LactoRun
        </a>
        
        <% if !user_signed_in? %>
            <!-- Botão de Ação -->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Alterna navegação">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <!-- Tudo dentro dessa div irá sumir -->
            <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item"> <!-- 'active' to active-->
                        <a class="nav-link" href="">Como funciona?</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="">Protocolo</a>
                    </li>
                </ul>
                <button class="btn btn-outline-info btn-block btn-nav mr-sm my-2 my-lg-0" type="button" data-toggle="modal" data-target="#modalEntrar">Fazer login</button>        
                <button class="btn btn-outline-info btn-block btn-nav my-0" type="button" data-toggle="modal" data-target="#modalRegistro">Inscrever-se!</button>
            </div>
        <% else %>
            <div class = "form-inline">
                <% if current_user.is_manager %>
                    <button class="btn btn-navbar" style = "margin-right: 25px;" type = "button" data-toggle="modal" data-target="#modalFormNewTeam">
                        <i class="fas fa-plus" style = "color: white"></i>
                    </button>
                <% end %>
                
                <div class = "dropdown">
                    <button class="btn" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style = "padding: 0px">
                        <img src = "/assets/user-simple.png" class = "navbar-circle mr-sm">
                        <i class="fas fa-sort-down" style = "color: white"></i>
                    </button>
                    
                    <div class = "dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton" style = "margin-top: 15px">
                        <div style = "padding-left: 24px; padding-right: 24px;">
                            <table>
                                <tr>
                                    <td><img src = "/assets/user-simple.png" height = "75" class = "mr-sm"> </td>
                                    <td>
                                        <%= current_user.name %><br>
                                        <small><%= current_user.email%></small>    
                                    </td>
                                </tr>
                            </table>
                            <%= button_to "Editar Perfil", edit_user_registration_path, method: :get, :class => "btn btn-block btn-sm mt-sm btn-outline-info" %>
                        </div>
                        <hr>
                        <%= button_to "Sair", destroy_user_session_path, method: :delete, :class => 'dropdown-item'%>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</nav>